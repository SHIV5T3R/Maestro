
import {
   bundleDirectory,
   bundledAssets,
   cacheDirectory,
   documentDirectory,

} from 'expo-file-system';

export const MAIN_BUNDLE_PATH = bundleDirectory;
export const MAIN_BUNDLE_ASSETS = bundledAssets;
export const CACHES_DIRECTORY_PATH = cacheDirectory;
export const DOCUMENT_DIRECTORY_PATH = documentDirectory;
